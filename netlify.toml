[build]
  command = """
    curl -LsSf https://astral.sh/uv/install.sh | sh &&
    export PATH=\"$HOME/.local/bin:$PATH\" &&
    uv sync --group docs &&
    uv run mkdocs build
  """
  publish = "site"

[build.environment]
  PYTHON_VERSION = "3.11"
